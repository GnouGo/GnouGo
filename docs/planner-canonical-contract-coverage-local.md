# Canonical contract coverage: fresh LOCAL result

## Outcome

**VERIFIED QUALIFICATION BLOCKER — category C. LOCAL stopped.**

Exactly one fresh LOCAL ran as `schema5-canonical-contract-coverage-diagnostics-1:local` on frozen production `ada9dbe35424f85e2e45ff1161bfd890960ce468`. Production sources, binaries, fixtures, packages and prior archives remain unchanged. No production patch, replacement start, retry, MIXED or Stage 1 followed.

Admission committed one required `local_processing` operation, but its support set contains semantically invalid descriptive evidence. A structurally committed proof does not satisfy the LOCAL acceptance contract. This is not a support-cardinality defect.

The complete redacted manifest, accounting, proof fingerprints, contribution references and original-schema receipt audit are in [the JSON report](planner-canonical-contract-coverage-local.json).

## First meaningful blocker

The first invalid qualification is request **9**, decision `contribution_runtime_1056b10f15fc176a02002c7a`, in `intent_operations`. The terminal fixture assessment reported `DIAGNOSTIC_DESCRIPTIVE_SUPPORT`:

> Property evidence must be completely attached as governing evidence and must not authorize executable support.

The response qualified owned span `[579,599)`, **“business processing.”**, as `supports` with basis `requested_result_production`, targeting `operation_e226ff9ed819b5a645e7d9de`. Its complete clause is:

> This is deterministic, local, in-memory business processing.

That clause describes execution properties. The selected noun phrase does not request performance or result production. A valid effect ID, declaration owner and positive-basis enum do not make it execution evidence.

The affected contribution is `contribution_3a8d2c45d77e69c132a62d14`, owned reference `r_2f8d5489bb6750cc73a60438`. Its qualification proof fingerprint is `25dd77def0fade7a5f745afa07eb60548a1ac9ed4e3ec409ab319e0321cc5d61`. Request and receipt SHA-256 fingerprints are respectively `93dd703a00455259acbfe98fb0fe9316b6a5709e9b2546b664522825f0f5782c` and `98ce7363e76ee3a1374413ac68f86a22b83ca28da166a37e96f4114ba45ef0df`.

The answer satisfies its original schema. The existing harness detected the semantic defect after admission, following all sixteen calls. No online check rejected request 9 at dispatch time. Once observed, no further provider request was made.

## Root cause

Canonical qualification split the descriptive clause into an excluded prefix, a governing adjective span, and an executable-support noun phrase. Coverage then consumed the current qualification proof as designed. The initial semantic qualification assigned stronger authority than its owned evidence establishes.

This is distinct from the previous declaration-coverage blocker. The descriptive clause is not canonical declaration evidence; role-neutral contract coverage cannot establish whether eligible prose actually requests execution. Eligibility and correctly resolved ownership remain necessary but insufficient for support.

The run produced seven support contributions and eight governing properties on one effect. Multiplicity is valid in principle. The failure is the meaning of the identified support, not the number of supports or operations.

## Authority analysis

The engine established owned references, exact coordinates, canonical declaration coverage, effect identity, scope and positive-basis reference validity. The model owned semantic qualification of requested execution versus properties. Historical `EvidenceRole` values granted no authority and were not populated in this run.

The available typed facts do not independently prove that this eligible sentence requests work. The fixture's semantic review establishes that it describes existing work instead. No generic deterministic exclusion based solely on the current typed contracts has been established here; a keyword veto, policy-label precedence or same-effect shortcut would not justify a correction.

The authority owner is **canonical execution-contribution qualification**. This repeats the executable-support authority failure class after qualification and applicability changes. It triggers architecture review before any further production change. Declaration coverage, necessity, identities, dependency authority and governing applicability must retain their existing invariants.

## Proposed action

Stop live validation and review executable-support authority, including whether the issued subspan domain can preserve the complete clause's request-versus-description semantics. No prompt change, parser promotion, targeted noun filter, additional retry or larger budget is proposed in this gate.

Preserve the stopped campaign and its exact requests, original schemas, responses, checkpoint and cumulative accounting. No automatic isolated experiment is justified by this run: every provider request completed with verified usage.

## Validation performed

**Offline prerequisites rerun:** 3,682 solution tests passed, one optional provider-test skip, zero failures across 29 assemblies. This includes 1,085 planner, 855 Flow.Core and 544 Agent.Server tests. The focused harness selection passed 199 tests. Harness and test builds disabled rebuilding production references and completed without warnings or errors.

The reference selfcheck passed six classifier/batch cases and eighteen frozen fixtures. Published Native AOT planning/encrypted-runtime/baseline-restart smokes and the trimmed Agent.Server persistence smoke passed. Existing production build, frontend, package and publish evidence was retained and hash-verified. The earlier implementation result of 3,680 passing tests and one optional skip is separate retained evidence.

**Synthetic fixtures:** offline harness LOCAL and MIXED selfchecks passed with zero provider calls and successful synthetic restart assertions. These do not establish live convergence.

**Live:** the one authorized LOCAL used the unchanged `gpt-5.5-2026-04-24`, all-low reasoning, sixteen-reservation allowance, 12,000 input ceiling, 9,600 dispatch target, 8,192 output ceiling and existing bounded 16,384 singleton escalation. One USD/EUR prerequisite check succeeded with quote date 2026-09-16; no quote was injected. Eight initial interpretation pages were frozen before dispatch.

Typed host policy projected eight clauses into nine obligations with zero host interpretation decisions. Typed baseline projection accounted for five structural units with zero baseline interpretation decisions and zero baseline operations. There were zero unresolved runtime roles. Canonical declaration fixture ports and attachments remained `record` (required), `threshold` (optional, omission default 100) and `classifiedResult` (required), with preservation retained as declaration evidence.

**Read-only evidence audit:** all sixteen returned JSON answers validate against their original request schemas, with zero findings. Public KeyVault APIs were used throughout. The audit made zero provider calls and zero checkpoint writes; current checkpoint and budget fingerprints remained unchanged. It did not reinterpret old answers under changed schemas or replay admission with synthetic responses.

**Live restart validation: not reached.** The fixture check failed before applicability chronology validation and completed-admission restart. Reading the persisted checkpoint and validating original response schemas do not establish a successful zero-call admission re-entry.

All sixteen recorded source hashes, twenty-three original tested DLL hashes, two published binaries, four packages and two fixture-source hashes still match the implementation report. The isolated harness DLL hash is `62a1bca775d99e046d3e533f62afd1e32c7d6f6fb82cfcc8eaf30acb400c671f`. The production DLL-set fingerprint remains `aa727025b0c5edff49faa1b64f99c7c9754d51cd28050a4cba5cd2f364ccf9ab`. Prior archive fingerprint `4a08b0e0b80f7f40042bdec4cdfdb2212b51f626946253adedb7eaae1417a774` is unchanged. Per-file checks and log hashes are retained in the JSON report.

## Decision accounting

| Work | Logical model decisions | Verified calls | Input tokens | Output tokens | Reasoning tokens |
|---|---:|---:|---:|---:|---:|
| Interpretation | 8 | 8 | 24,748 | 2,980 | 1,392 |
| Contribution qualification | 6 | 6 | 6,166 | 2,479 | 1,473 |
| Realization coverage | 1 | 1 | 1,746 | 244 | 113 |
| Governing applicability | 8 | 1 | 2,426 | 1,050 | 359 |
| Boundary scope | 0 | 0 | 0 | 0 | 0 |
| Occurrence identity | 0 | 0 | 0 | 0 | 0 |
| Operation dependencies | 0 | 0 | 0 | 0 | 0 |
| Downstream relationships | 0 | 0 | 0 | 0 | 0 |
| **Total** | **23** | **16** | **35,086** | **6,753** | **3,337** |

Output includes reasoning; derived non-reasoning output is 3,416 tokens. Usage is known for all dispatches. Eight runtime-facet decisions are embedded in interpretation and add no separate calls. Eight model applicability decisions were packed into one call; subsequent eight deterministic attachment projections are not eight deterministic semantic applicability decisions.

The checkpoint contains one deterministic canonical identity assignment, nineteen deterministic source/declaration exclusions, seven model-qualified supports, eight model-qualified properties and one model-qualified exclusion. Zero runtime action occurrences required the canonical-declaration exclusion path in this specific LOCAL response; the broader deterministic exclusion count includes policy, baseline and non-executable contract records. This run does not establish live MIXED exclusion behavior.

Canonical operation: `operation_e226ff9ed819b5a645e7d9de`, required `local_processing`, one `main` result realization. Inputs are `decl_89cf60869b3c67ae8d948cde` (`record`) and `decl_d9dced873ca13886d395895e` (`threshold`); output is `decl_e10c86997aef941cbd55d2f1` (`classifiedResult`). No external, lifecycle or human operation exists. The admission-owned operation-producer set and `Effect.Producers` are empty. No dependency-model or standalone identity decision was issued.

Recorded fingerprints:

- Aggregate admission: `b01160e50d87b503c0d2183e6ed6df84ed91830272d1caa248cd91fc9da9d432`.
- Operation admission v13: `83504251b5ab35b6c7fdebb566ebd01d4afd4f4da3f6aacb852040e305bf7e0c`.
- Coverage v3: `c1c6076a7ab8ef40dae6b03181d730d0d44d8c77afaa76275a33f98514a41425`.
- Dependency v1: `a48920eebd5a20679f72ee06047aa174e83d9a481c2bde097f67171dff1b4ad1`.
- Persisted checkpoint: `b435e8aafe04f22f81db5afefdac4e50bf924b46f518188bf3613c474b4d4c58`.

There are sixteen coordinator reservations, sixteen journal requests and sixteen verified receipts, with no unmatched reservations. Partitions, escalations, semantic repairs and unverifiable requests are all zero. Largest estimated input was 5,549 tokens; largest actual input was 4,001 tokens. Remaining durable allowance is **zero** at admission and at the stop.

The full allowance was consumed by eight interpretation and eight admission calls. There was no budget-exhaustion exception or queued semantic decision at the stop; the terminal blocker was semantic qualification. Live interpretation yielded six executable candidates, unlike the smaller synthetic contribution shape. No limits were increased. Production planner decisions added or removed in this gate: **zero**. No provider-call savings are inferred from offline envelopes.

## Next gate

**Architecture review.** MIXED and Stage 1 remain **not run**.
