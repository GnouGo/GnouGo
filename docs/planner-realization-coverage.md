# Complete realization coverage — offline validation

## 1. Outcome

**PASS — implementation and offline validation.** Admission now derives operations from complete effect-owned coverage, without a privileged realizing sentence. No live LOCAL, MIXED or Stage-1 diagnostic was started, and no provider request was made.

Implementation began at repository head `dbc2e10c1ddc93b80649aeab444fce01730ff601`, following the approved review of frozen production `cfc058fd52183854d09b2540b9f57d1e337797f9`. The [machine-readable evidence](planner-realization-coverage.json) records the changed source and tested artifact hashes, original receipt audit, strict replay, synthetic corrected results and validation logs. These results establish offline admission behavior, not live convergence or executable Stage-1 success.

## 2. First meaningful blocker

The retained MIXED checkpoint `schema5-occurrence-boundaries-mixed-diagnostics-1:mixed` stopped with `INTENT_OPERATION_UNRESOLVED` at `/operations/@runtime_082f2a85c350348c8f13eb21`. Its classification contribution selected `governing`, leaving the local result without a realization. The independent read contribution did establish an external effect. Governing validation correctly refused to create the missing local operation.

All ten original responses satisfy their original schemas. Before modification, completed-page replay reproduced the same stop with zero provider dispatches and unchanged checkpoint/accounting. The root cause is category C: the issued independent contribution domains allowed a completed assignment with missing required realization coverage.

## 3. Root cause

Canonical IDs already belonged to effects and independently evidenced boundaries. Realization authority still depended on individual contribution answers and the first assignment in the admission record. Several compatible descriptions could all defer, and later validation had no complete-set executable authority to use.

The engine knew the required contributions and their finite effect domains. It could prohibit an uncovered completed assignment, but could not promote a returned governing answer into executable authority. The correction therefore changes the coverage contract, rather than rewriting a model answer.

## 4. Authority analysis

The pre-patch self-review was:

1. Required executable contributions could remain uncovered.
2. Canonical admission owns complete realization coverage.
3. The engine already knew source ownership, necessity, exclusions and effect domains.
4. Independent answers exposed collective deferral.
5. Complete bounded alternatives eliminate that completion before dispatch.
6. The implementation uses owned references and typed execution facts, without fixture wording.
7. Independent occurrences, optionality, baseline authority, scope and revisions must remain intact.
8. Independent/shared-endpoint, outputless, repeated and resource-operation regressions preserve separation.
9. Effect-owned aggregate authority replaces privileged-root selection; semantic executable support and dataflow still require assessment.
10. Another verified coverage-authority blocker requires architecture review before another targeted filter.

Source text remains evidence. A public output, compatible kind or governing contribution alone cannot create an operation. Necessity remains a contract attribute. Exact baseline coordinates and independently proven occurrence boundaries retain their existing identity calculation. Dependency proof remains the single authority for operation-to-operation data edges.

## 5. Implemented action

`PlanningOperations.RealizationCoverage` derives connected coverage domains from eligible owned evidence. Groups couple decisions but do not merge effect identities. Each indivisible decision offers strict alternatives with a fixed realized-effect set, an issued complete mapping ID, and per-effect public dataflow restricted to that effect's workflow. Mapping IDs resolve to explicit supporting, governing or optional-omission evidence records; they are not operation IDs or invented descriptions.

Every selected effect has executable supporting evidence. Required contributions cannot answer `not_an_effect` or collectively defer. Governing and shared mappings target selected realized effects only. Independently owned boundaries retain their own coverage requirements. Known forced necessity conflicts have no completable mapping and stop before dispatch. A group that cannot fit the existing request/answer sizing policy stops as indivisible; it is not split into partially acceptable coverage fragments.

Optional omission requires owned optional necessity. Entire optional cohorts can record omission; independently proven optional occurrences may be omitted within a coupled group. Omission cannot hide required/optional conflicts. Omitted outcomes remain in durable coverage decisions and the global admission fingerprint, including when no operation is admitted. The existing executable-intent guard remains unchanged.

Operations are materialized from aggregate supporting and governing assignments. Assignment ordering has no authority. `AnchorReference` is selected deterministically for diagnostics and source attribution; canonical IDs still derive from effect ownership and occurrence boundaries. Exact baseline nodes supply deterministic coverage. Jointly established governing applicability is reused; only remaining applicability uses the existing realized-effect governing path.

Current proofs revalidate coverage, scoped dataflow, necessity, revision retirement, dependency readiness and the complete surviving operation set before atomic commit and on restart. Removed operations cannot reappear through retained coverage pages. Optional omissions cannot disappear through missing pages. No second graph, authoritative collection, runtime interface, service, command envelope or phase was added.

Proof versions are realization coverage **1**, effect **5**, admission **10**. Occurrence **1**, source/runtime **6**, declaration **5**, dependency **1**, baseline projection **1**, and Schema-5 storage remain unchanged. Source-generated serialization includes the new proof records. Historical proofs require explicit reassessment; archived receipts, budgets, corrections and escalation records are not migrated or reset. No reasoning, token ceiling, partitioning or escalation changes were made.

## 6. Validation performed

**Historical:** the original-schema audit verified all ten responses and reproduced the retained stop before implementation. Current-request strict replay then stopped at `REPLAY_EVIDENCE_REQUIRED`, with zero matching receipts reused and zero provider calls. Historical responses were never interpreted under the new schema. Both archive checks report unchanged checkpoint and budget records.

**Synthetic corrected evidence:** detached copies of the retained LOCAL and MIXED checkpoints supplied unchanged runtime facts, declaration ports and attachments. New coverage/dataflow answers are explicitly synthetic. LOCAL proves one required classifier operation consuming `record` and `threshold`, producing `classifiedResult`, with classification/fallback and descriptive evidence retained. MIXED proves one required external read consuming `sourceId`, one required classifier consuming `threshold` and the read result, and exactly the admission-owned read → classifier edge. Preservation remains declaration evidence. Both preserve canonical IDs and pass detached read-only restart with zero calls, zero writes and identical snapshot/admission/dependency fingerprints.

**Automated gates:** 3,579 solution tests passed across 29 assemblies, with zero failures and one optional provider-test skip. Planner: 1,038 passed. Focused operation tests: 156 passed; focused harness tests: 79 passed. Solution and harness builds, Agent/Flow frontend builds, and four affected package builds completed without emitted warnings. Native AOT planning/encrypted restart and trimmed Agent.Server persistence smokes passed, including coverage serialization and optional omission evidence. Reference selfchecks passed six classifier/batch and eighteen additional frozen cases. Existing publish-local Jint and framework exceptions remain unchanged and documented in component READMEs; no new suppression was added.

Regressions cover complete required coverage, aggregate support without index-zero authority, explicit omission, conflicts, shared governing target sets, independent/outputless/resource effects, baseline ownership, scoped/foreign references, stale proofs, revision retirement, changed page packing, reversed evidence enumeration, partial checkpoints, no redispatch on completed restart, dependency reuse, cancellation and existing bounded output/correction behavior. Domain assertions run before synthetic answers.

An initial parallel solution test build encountered a shared `GenerateDepsFile` output lock. The final solution build ran serially and the complete tests ran without rebuilding. Final evidence uses only successful runs.

## 7. Decision accounting

| Measure | Retained MIXED checkpoint | Corrected synthetic checkpoint |
|---|---:|---:|
| Interpretation decisions / initial pages | 9 / 9 | 9 / 9 |
| Independent contribution-status decisions | 3 | 0 |
| Joint coverage decisions | 0 | 2 |
| Action contributions requiring semantic assessment | 3 | 3 |
| Privileged realizing-root authority | Present | Removed |
| Standalone occurrence-identity decisions | 0 | 0 |
| Dependency decisions | Not reached | 2, existing bounded data/none path |
| Additional governing decisions | Not reached | 0; applicability included jointly |

The two MIXED coverage decisions pack into **one** initial request at the unchanged limits. Their schema sizes are **891** and **961** UTF-8 bytes. Synthetic request estimates are **2,328 input tokens** for coverage and **1,624** for dependencies. These are estimates, not provider usage.

Retained LOCAL's two independent contribution decisions and later governing decision become **one joint coverage decision**, packed into one request. Its schema is **961 bytes**, with an estimated **1,737 input tokens**. It retains two action-evidence assessments, zero standalone identity decisions and zero dependency decisions. The resulting operation has one supporting contribution and one governing attachment. MIXED has two supporting contributions and one governing attachment across its two operations.

Both fixtures use deterministic canonical identity assignment after grounding. No new boundary-scope question is introduced. No provider-call savings are claimed: historical and synthetic envelopes are different evidence, and output handling may change future live request counts. Actual live tokens for this iteration are unavailable because there was no live dispatch. Synthetic replies have no provider usage. No semantic repairs, output partitions or singleton escalations were needed by these corrected synthetic checkpoints.

## 8. Next gate

**LOCAL**, requiring subsequent authorization on a newly frozen implementation. Stop here. No live LOCAL, MIXED, Stage 1, behavior acceptance or construction campaign is included in this task.
