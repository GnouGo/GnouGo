# Joint clause qualification — offline implementation

## 1. Outcome

**IMPLEMENTED — OFFLINE VALIDATION PASSED.** Contribution proof **4** and admission proof **14** establish execution requests jointly for complete owned clauses. Production changes are based on repository head `941ba5e` and previous production `ada9dbe35424f85e2e45ff1161bfd890960ce468`.

No live LOCAL, MIXED, Stage 1 or provider request ran. Historical campaigns remain stopped. Detailed proofs, request measurements, validation logs and integrity hashes are in [the machine-readable report](planner-joint-clause-qualification.json).

## 2. First meaningful blocker

The retained LOCAL request 9, `contribution_runtime_1056b10f15fc176a02002c7a`, selected “business processing.” `[579,599)` as executable support after splitting the complete descriptive statement into unrelated qualifications. Its original schema accepted the response; LOCAL stopped at `DIAGNOSTIC_DESCRIPTIVE_SUPPORT` after structural admission.

This remains **category C: schema-valid, semantically invalid qualification**. Neither the historical outcome nor its original report has been rewritten.

## 3. Root cause

Independent runtime-fragment decisions accounted for characters without preserving their relationship to a request for performance or result production. Overlapping runtime records could also qualify one statement repeatedly. The retained six candidates belong to three complete clauses, with membership **1, 4, 1**.

The new boundary removes independent fragment support choices. It cannot make semantic interpretation infallible: a model may still misinterpret a complete statement. Tests distinguish structural enforcement from the explicitly synthetic semantic answers supplied to it.

## 4. Authority analysis

1. Descriptive fragments cannot independently establish requested execution.
2. Canonical contribution qualification owns this boundary.
3. The engine knows source ownership, complete clauses, eligibility, locked execution facts and finite effect domains.
4. Previously, each preliminary fragment independently received executable alternatives.
5. The engine now groups the clause once and projects support only through a qualified execution unit.
6. Grouping, ownership and validation use references and coordinates; production contains no fixture vocabulary or provider branching.
7. Mixed meanings, conditional results, independent occurrences and necessity conflicts remain expressible and validated.
8. Separate, owned actions in one clause retain separate operations, including overlapping evidence for independently established occurrences.
9. Clause semantics remain model-owned; support projection, provenance aggregation and canonical identity remain engine-owned.
10. Another verified executable-support authority failure requires architecture review before a targeted correction.

A shared clause does not merge occurrences. An issued effect restricts support provenance; every runtime member must still receive complete accounting. Conflicting compatible necessity or ownership facts cannot disappear through selection of a first parent. Property applicability retains its independence from preliminary execution kind.

## 5. Implemented action

After current declaration exclusions and occurrence grounding, one indivisible decision covers each complete source-owned clause. Its bounded units are requested execution, governing property, no operation relevance, or unresolved. A requested unit binds its predicate, complete-clause scope, supporting/result-selection references, effect, positive basis and ownership/boundary references. The parser derives support contributions with explicit unit links. It rejects missing or foreign links, an unaccounted predicate, contradictory authority and incomplete runtime-member coverage.

The response supports up to six units and four support references per execution unit within the existing answer target. Insufficient capacity stops; paging cannot split a clause into independently acceptable fragments. No token, reasoning, call-budget, partition or escalation setting changed.

The resulting proof IR stays on existing admission records:

| Existing record | Added proof/evidence |
|---|---|
| Execution-contribution proof v4 | Owned clause, sorted runtime membership and qualified semantic units |
| Semantic unit | Request predicate/scope, owned projections, complete provenance and fixed effect/support basis |
| Contribution | Unit link and all applicable runtime provenance bindings |
| Operation assignment | Full provenance bindings; the legacy scalar parent is diagnostic |
| Admission v14 | Current qualification and all downstream proof fingerprints |

Coverage consumes the fixed projected supports. Governing applicability still follows realization, dependencies retain admission-only authority, and atomic commit revalidates the reconstructed qualification. Canonical operation IDs remain unchanged. Original fragment receipts cannot satisfy new clause requests; correction source identities retain member lineage and existing cumulative allowances/accounting are preserved.

Coverage **3**, applicability **1**, effect **7**, occurrence/dependency/baseline projection **1**, runtime **7**, source **6**, declaration **5**, and **Schema-5** remain unchanged. No graph, authoritative snapshot collection, planner phase, runtime interface or public command was added. The added harness command is offline-only: `audit-joint-clause-fixture`.

## 6. Validation performed

**Full offline suite:** **3,689 passing tests, one optional provider-test skip, zero failures**, across 29 assemblies. This includes **1,092 planner**, **855 Flow Core**, and **544 Agent.Server** tests. Seven clause regressions and 220 focused harness checks also passed; these overlap the suite and are not added to its total. The clause regression was strengthened afterward to check inherited branch predicates and reversed support enumeration, and all seven passed again.

Solution and harness builds, Agent/Flow frontend builds and four component packages passed. Published Native AOT planning/encrypted-restart and trimmed Agent.Server persistence smokes passed. The trimmed fixture round-trips clause units, predicate links and multiple provenance bindings through generated serialization. Reference selfchecks verified six classifier/batch cases and eighteen frozen fixtures without provider or business transport. Existing documented framework/Jint trim/AOT exceptions remain unchanged; no suppression was added. Publishing emitted one informational package-cache message, recorded in the log, with no compiler/linker warning or failed check.

**Historical evidence:** all **16** retained answers still validate under their **original schemas**, with zero findings. Current-proof re-entry stops at `INTENT_OPERATION_PROOF_MISSING`, with zero provider calls and zero replayed receipts. No changed request is matched to an old answer. The archived LOCAL checkpoint remains `b435e8aafe04f22f81db5afefdac4e50bf924b46f518188bf3613c474b4d4c58`.

**Synthetic LOCAL:** exactly one required `operation_e226ff9ed819b5a645e7d9de` (`local_processing`), consuming `record` and `threshold` and producing `classifiedResult`. Two qualified execution units project two supports onto that effect. The complete rules support retains its conditions and fallback; its four overlapping runtime records share the request linkage. One descriptive unit attaches through a separately proved governing applicability decision and supplies no support. Preservation remains declaration evidence. There are no external, lifecycle or human operations, identity decisions or dependency-model decisions.

**Synthetic MIXED:** required external read `operation_6b515f252d71b693829d2fe4` consumes `sourceId`; required local `operation_e226ff9ed819b5a645e7d9de` consumes its result and `threshold`, producing `classifiedResult`. Each has one positively qualified support. Admission alone establishes read → local, with owned semantic evidence and no self-edge, cycle or invented operation. The complete read/rules supports retain their owned semantics without duplicate property records. The downstream relationship domain contains no `data` alternative.

Both synthetic cases retain the supplied declaration ports/attachments, optional threshold/default `100`, and exact canonical identities. Both validate contribution v4, coverage v3, effect v7, dependency v1 and admission v14 after reload; LOCAL also validates applicability v1. Completed re-entry uses **zero calls and zero checkpoint writes**, with identical snapshots, IDs, proofs and accounting. These are explicitly synthetic corrected fixtures, not live convergence or retrospective campaign acceptance.

All 23 preserved diagnostic DLLs, original reports, retained packages/publishes and declaration fixtures remain unchanged. Both fixture audits independently confirm unchanged checkpoint/budget records and the same aggregate archive fingerprint: `4a08b0e0b80f7f40042bdec4cdfdb2212b51f626946253adedb7eaae1417a774`.

## 7. Decision accounting

| LOCAL measure | Retained live checkpoint | Synthetic current checkpoint |
|---|---:|---:|
| Interpretation decisions / initial pages | 8 / 8 | 8 / 8 |
| Independent fragment qualification decisions | 6 | 0 |
| Joint clause qualification decisions / pages | 0 | 3 / 3 |
| Execution-request units | Not represented | 2 |
| Governing-property units | Independently fragmented | 1 |
| Support projections | 7 | 2 |
| Coverage decisions / pages | 1 / 1 | 1 / 1 |
| Applicability decisions / pages | 8 / 1 | 1 / 1 |
| Standalone identity decisions | 0 | 0 |
| Dependency-model decisions | 0 | 0 |

The established scope reduction is **six fragment qualifications → three clause qualifications**, accounting for all six records. The synthetic decomposition uses three semantic units and two explicit execution-request relationships. Different legitimate decompositions may produce different support/property counts. Regrouping does not eliminate the underlying semantic work, and no saved provider calls are claimed.

Measured through the unchanged packer, LOCAL requires **13 nominal requests**, leaving **3** slots. MIXED uses nine interpretation requests, two qualification requests, one request packing two coverage decisions, and one request packing two dependency decisions: **13 through admission**. Its eight downstream relationship decisions pack into one further request: **14 total**, leaving **2** slots. MIXED has no separately qualified properties in this synthetic decomposition; that is not a general deterministic-applicability claim.

LOCAL qualification schemas are **2,673 / 6,173 / 2,553 bytes**; MIXED schemas are **3,181 / 2,886 bytes**. The largest measured post-interpretation input envelopes are **3,309** estimated tokens for LOCAL and **2,119** for MIXED. Nominal packing is not a guarantee against future truncation, repairs or escalation.

This iteration made **zero provider calls** and used no provider tokens. Synthetic requests have no actual provider usage. The historical LOCAL remains sixteen verified calls, **35,086 input / 6,753 output / 3,337 reasoning tokens**, with zero partitions, escalations, repairs or unverifiable calls. Archived accounting was not reset or replenished.

## 8. Next gate

**One fresh LOCAL, requiring separate authorization.** Stop after offline validation. No live LOCAL, MIXED or Stage 1 is included or authorized by these results.
