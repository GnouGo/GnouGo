# Attachment-only declaration constraints

The correction starts from `369ac23`. It adds `declaration_constraint` to source
interpretation and the existing `intent_declarations` attachment domain. Public
roots, behavior assembly, model profiles, paging, output escalation, the compiler
and runtime interfaces are unchanged.

A constraint selects only `modifier_of` an issued canonical target or `unresolved`.
It cannot create a declaration, alias it, assign a default or change direction,
scope or presence. Root presence choices exclude constraint-only evidence. Existing
materialization retains the selected evidence as modifier references and complete
clauses; existing behavior assembly renders them in the canonical port description.
Unresolved, unlocated or stale attachment proof stops before review. Real supplied
`explicit_value` obligations remain outside this path, and host constraints grant
neither operation nor public-port authority.

Source and declaration proofs are version 4. Schema-5 databases and encrypted
namespaces remain unchanged. Historical classifications and receipts are audit
evidence; no production conversion or migration is provided.

The retained Stage-1 session `36cc6d8ce339488184bd30ea22c6c04f` reached review at
revision 44 with correct public roots but unattached category enum evidence. Before
the correction, strict replay from revision 30 reproduced BehaviorReview with one
receipt, zero provider calls and unchanged persisted state. The historical clause
remains `explicit_value`; its archived response has not been replaced.

`DeclarationConstraintTests` uses a sanitized capture of that declaration state.
The test explicitly supplies synthetic corrected classifications and attachment
assignments while preserving the captured root assignments, source names, direction,
presence and threshold default. It proves that the enum and preservation clauses
become part of the canonical output's modifier evidence and behavior description,
without another port. These responses are not historical receipts or evidence of
live model convergence.

Generic tests cover renamed inputs/outputs/members, actual supplied values, type and
nullability evidence, host and baseline authority, strict response alternatives,
incomplete/foreign/stale proof, missing targets and restart between completed roots
and attachments. The published Native AOT smoke also exercises this attachment kind
through encrypted persistence and receipt replay.

The separately authorized campaign is `schema5-declaration-constraints-stage1-1`:
one fresh classifier session through exact behavior acceptance, construction and
bounded repair, mandatory validation, six independent execution cases and exact
artifact approval. The first meaningful blocker stops the campaign. No replacement,
unverifiable redispatch, reasoning experiment or Stage 2 is permitted.

Offline validation passed: 3,264 solution tests across 29 projects (one optional
live-provider test skipped), including 816 planner tests and 30 new constraint
regressions. All 53 focused harness tests and the reference-fixture selfcheck passed.
The serialized solution build, four affected packages, Native AOT planning/encrypted
restart and trimmed Agent.Server EF persistence publish/smokes completed without
warnings under the existing documented publish exceptions. No suppressions were
added. The full evidence index is [recorded separately](planner-declaration-constraints-offline.json).

Strict replay on current proof version 4 stops at obsolete source-authority proof
with no model dispatch or substituted receipt. The archived snapshot remains
unchanged. Corrected fixture materialization is a separate synthetic evidence track.
