# Semantic grounding live acceptance

Implementation and acceptance are in progress on `feat/semantic-grounded-planner`, based on `feat/deterministic-planner-v2` at `b0d56c791ad0b330f4eb8646f6941a84dddf79b0`. The base branch is unchanged.

Private requests, model responses and session artifacts remain in encrypted KeyVault records. The table records real model planning attempts; scenarios, when present, use simulated integrations. No generated workflow has been approved or executed.

| Attempt | Source | Session | Coverage | Calls / replans | Outcome |
| --- | --- | --- | --- | --- | --- |
| Designer 1 | `d09e53a` | `a636cc4b80e94167ac224fe323485814` | 129 capabilities, 3/3 pages | 4 / 0 | Stopped before binding: input limit |
| Designer 2 | `9552c17` | `fc81bfc29dc6484088e9f151bd81f2b3` | 129 capabilities, 3/3 pages plus selection | 5 / 0 | Stopped before binding: 13,952 estimated input tokens |
| Chat 1 | `9552c17` | `e93b2a8a08d492a57718b775fb26e4705d81450067882db5514f235fafbf4859` | 129 capabilities, complete catalog page | 3 / 1 | Clarification requested execution approval; left unanswered |
| Designer 3 | `a60d66e` | `832b1dc1a9f34bb4aeb0b762f7f4d83b` | 129 capabilities, 3/3 pages plus selection | 5 / 0 | Stopped before binding: 12,690 estimated input tokens |
| Chat 2 | `a60d66e` | `dbd0d8b1e9b03b32a86e8c9186fecf2d34fb1070a56c9c7d16166b12fd3a0f74` | 129 capabilities, complete catalog page plus selection | 8 / 4 | Stopped: binding contract failures, call allowance exhausted |
| Designer 4 | `164db35` | `bb9ef77b7c034ef99353052e50ef74da` | 129 capabilities, 3/3 pages plus selection | 6 / 0 | First binding batch exhausted output allowance during reasoning |
| Chat 3 | `164db35` | `b3b19e936c48c4e672d2e3762741f8e8b36fd89f6a7c45dff1ce3a2fcf67f73c` | Not reached | 1 reserved / 0 | Harness opened a new conversation during dispatch; uncertain reservation retained |
| Chat 4 | `164db35` | `439d4b185b19d44ec880c61087f5a73f5f673e4edcccdb51c1875a8cf531cf32` | 129 capabilities, complete catalog page plus selection | 8 / 4 | Stopped on a host oneOf compatibility proof defect |
| Designer 5 | `4ab9f7f` | `493c516907254c8bb521ae86d89357dd` | 129 capabilities, 3/3 pages plus selection | 7 / 0 | Two binding batches accepted; remaining context exceeded the final call allowance |
| Chat 5 | `4ab9f7f` | `47527f0e6a5f776c3c555fe14d6ad710d114c314a0adadd25950866d7152ce96` | 129 capabilities, complete page plus selection | 8 / 3 | Binding corrected; host cleanup-output validation failed after fixtures |


Designer prompt: 680 UTF-8 bytes; SHA-256 `8f9c138913d4fd8e136760df3b7e20fe4cf7623f02fa9fa4c46220bcaae1cf85`. The actual Designer form submitted it unchanged at 2026-09-21 21:35:50 UTC. Provider/model/protocol: OpenAi / `gpt-5.5-2026-04-24` / Chat Completions, verified before dispatch. Settings: medium reasoning, 12,000 input tokens, 8,192 output tokens, eight calls, two replans. All four calls have completed durable receipts; no uncertain dispatch remains. Reported usage: 23,658 input and 15,254 output tokens, estimated EUR 0.501227. Catalog hash: `68e8ae283beaff5e87fad93d7d5d3874a6c1737acbc7bcd76324b3f405be6382`.

The first attempt completed semantic planning and every catalog page. Binding was rejected before reservation because the combined request required an estimated 25,094 tokens. Corrections preserve complete semantic coverage, introduce explicit selection among retained viable matches before loading binding contracts, omit repeated schema annotations from the binding view while retaining every assertion/default, and encode prompt Unicode literally. Business calculations may also ground to specialized declared evaluators, and leaf cleanup actions participate in grounding. Limits and authoritative catalog contracts remain unchanged.

Initial validation: 104 Planning, 853 Flow, 76 Flow.Integrations, 368 Agent.Server and 222 AI.Core tests passed in their latest runs. Warning-free solution build, frontend build, planner package, Native AOT eight-case corpus and trimmed Server EF/SQLite encrypted persistence smoke passed. These checks will be repeated for the final acceptance revision where affected.

Historical two-call measurements describe the old architecture. The current Native AOT corpus uses three calls for its six smaller cases and four calls for both complex review cases, with zero replans. Paged grounding, ambiguous-match selection, bounded binding batches, fixtures and replanning consume the same total allowance.

Designer 2 was submitted at 21:49:39 UTC with the same prompt hash and unchanged settings. All five receipts completed. Reported usage: 28,772 input / 16,972 output tokens; stored estimated cost EUR 0.568338. Semantic selection completed, but the combined binding schema/context still exceeded the input ceiling. Further corrections factor the response schema's common operation fields without removing assertions, omit semantically empty context defaults, and remove the obsolete grounding-stage questions interface.

Chat 1 was submitted through the actual chat composer at 21:49:49 UTC. Its unchanged 2,761-byte prompt hash is `6755b57df5804c26b9dca0c1babe62a31b9d2404ae7b5b96ff174cced1611649`. The generic acceptance runner preserves the existing medium reasoning, 64,000 / 32,768 request ceilings, eight total calls and six configured replans; only the renamed replanning setting changed. Reported usage: 27,529 input / 25,779 output tokens. Stored cost was unavailable (zero without an estimate), not evidence of free usage. Complete grounding identified an unsupported pre-execution approval action and unavailable exact check commands. Atomic replanning asked for execution permission. No answer or approval was supplied. Semantic generation now explicitly leaves FinalReview and execution approval to the host; missing command capabilities must remain fail-closed or be satisfied by a semantically valid alternative.

Designer 3 (22:10:56 UTC) and Chat 2 (22:11:42 UTC) used the same unchanged prompts and settings. Their catalog hash was `3635f655617248b7ec5ab9d4ec793264d70325dc44ade8a58d82eec44b109634`. All reservations have completion receipts. Chat 2 reported 112,526 input / 73,072 output tokens; its stored zero cost lacks an estimate. No scenario or approval was reached. Its trace is `2b29b688ed46a1d9bd78c53d2ff1565c`.

These attempts motivated complete-subgraph binding batches, removal of calculation type assertions, explicit opaque computation boundaries, and precise argument diagnostics. A host defect lost numeric literal values before checking bounded arguments; regression tests now preserve that proof through compilation while allowing alternate values for defaulted workflow inputs. Opacity checks now run before graph construction, including aliases and dynamic projections. No model, token, time or call limit was increased.

Designer 4 was submitted at 22:40:17 UTC. Usage was 33,955 input / 22,252 output tokens, estimated EUR 0.728751. Its binding request fit the input ceiling, but all 8,192 completion tokens were reasoning tokens and no binding JSON was produced. Complete subgraphs are now balanced across binding batches, with output-work estimates inside the unchanged ceilings.

Chat 3 was submitted at 22:40:58 UTC and cancelled by the harness's new-conversation action at 22:41:15 UTC. Its single dispatched call remains uncertain in encrypted storage; no receipt, usage or refund is invented. The corrected harness submitted Chat 4 at 22:43:55 UTC. Chat 4 completed all eight receipts, reporting 119,018 input / 71,798 output tokens; monetary cost remains unavailable. Its trace is `03e8a704728554d18cd2ae2c73b1395c`. All three attempts used the preceding catalog hash and unchanged model/settings.

Chat 4 narrowed binding failures to a generic JSON Schema inclusion defect: discriminator-based oneOf branches may omit type while still constraining objects. A regression now proves exclusivity for distinct required discriminators and rejects overlapping alternatives. Read-only replay exposed further boundaries: artifact requirements must appear in binding context, and optional transformation fields must retain their exact runtime schema. Authoritative contract references now avoid model duplication of large consumer records. Provider strict-format eligibility is computed separately from mandatory runtime instance validation; malformed or missing required transformation fields still fail. Cleanup output availability follows the runtime's successful-finalization rule and accepts only exact host-generated availability guards over unconditional completed producers.

Validation after these corrections: 132 Planning, 854 Flow, 76 Flow.Integrations, 374 Agent.Server and 222 AI.Core tests passed. The solution and frontend builds, planner package and all eight Native AOT corpus cases passed without warnings.

Designer 5 (23:12:40 UTC) completed all seven receipts: 39,599 input / 29,856 output tokens, estimated EUR 0.951849. Chat 5 (23:12:34 UTC) completed eight receipts: 119,770 input / 78,027 output tokens, monetary cost unavailable. Chat trace: `3d4d840b389e8b2b96917262351d3cc4`. Both retained the preceding catalog hash, unchanged prompts and configured settings; neither reached approval or external workflow execution.

Corrections preserve complete descriptions using compact catalog rows, factor repeated contract schemas through local references, omit private intermediate results from binding boundaries, and enforce empty later-batch declarations in the response schema. Read-only measurement of Designer 5 now yields two complete catalog pages; its formerly oversized final binding request fits the unchanged 12,000-token estimate. Semantic replanning reuses validated coverage only for unchanged actions and preserves complete output requirements. Dependency expansion includes captured producers, their consumers and named-subflow callers.

Core now proves successful cleanup-result availability from exact guards over guaranteed producer results. Compiler/host validation precedes fixture generation, and host disagreements stop without model replanning. Read-only replay also exposed infeasible scenario forcing: an availability guard was made false despite successful acquisition. Scenario coverage now fails each required producer and asserts the finalizer is skipped, while arbitrary business guards retain both outcomes. This is simulated regression evidence, not a successful live acceptance run. Array callback and unary-expression inference have separate boundary regressions.

The corrected host replayed Chat 5 from encrypted artifacts without model calls or external integrations: `FinalReview`, 67/67 simulated scenarios passed, no diagnostics. Final pre-publication checks for this iteration passed: 139 Planning, 856 Flow, 76 Integrations, 374 Server and 222 AI.Core tests; warning-free solution/frontend builds, planner package and eight Native AOT cases. [Sanitized attempt metadata and receipts](evidence/semantic-planner-live-attempts.json) retain dispatch and usage evidence without private payloads.

Designer 6 (`468e39b`, 23:48:38 UTC, session `6ae07a3accf049c7b9c16b1f6f82d465`) covered 129 capabilities in two complete pages, selected implementations and accepted its first binding batch. The checks subgraph exhausted all 8,192 output tokens during reasoning. Six receipts completed, zero replans; usage 33,368 input / 31,129 output tokens, estimated EUR 0.957972. Binding omitted meaningful parameter descriptions, including original-evidence prerequisites. Those descriptions now survive binding compaction, and selection sees artifact dependencies; binding preserves required-input descriptions. Binding may report located missing prerequisites for atomic semantic replanning without an executable proposal.

Chat 6 (`468e39b`, 23:48:27 UTC, session `48f5fad287245813baf4e9eb6670a7ee59f55722d0b7e84cbb2df811432d109d`) covered all 129 capabilities in one page, then exposed a host defect: a finalizer consumed an earlier finalizer's result without an availability guard. Eight receipts and four replans completed; usage 109,781 input / 67,251 output tokens, monetary cost unavailable. The correction guards chained cleanup dependencies and proves successful output availability transitively. Scenario fault injection verifies that a dependent finalizer never executes after its producer fails, including the runtime's wrapped finalization errors. Host lowering disagreements now stop immediately. Read-only validation of the saved Chat 6 plan passes compilation and host validation; this does not count as live acceptance.

Both attempts retain the preceding catalog hash, unchanged prompts and limits, and no approval. Detailed sanitized receipts are in the evidence file. The next live pair will use the same corrected source revision.

Designer 7 (`bc35553`, 2026-09-22 00:16:13 UTC, session `4f2bc1f2df3e415895507e5b0ef864a0`) covered three catalog pages, then replanned an unsupported hypothetical resource shutdown. Two new pages covered the changed action while retaining unchanged grounding evidence. All seven receipts completed; selection stopped before reservation at 15,663 estimated input tokens. Usage: 41,086 input / 19,306 output tokens, estimated EUR 0.682863.

Chat 7 (`bc35553`, 00:16:06 UTC, session `c07756e47b886f08a174fa2dbc380952f597170ae2c54012424ba98afa3775b8`, trace `ec372ee7dd2e77b6520bfd8579df47f5`) completed its catalog page but exhausted eight calls and five replans on duplicate/cross-action capability selections. Usage: 76,256 input / 26,347 output tokens; monetary cost unavailable. Both runs retained the preceding catalog hash and original settings. Neither reached scenarios or approval.

Selection now uses a required per-action capability map: an identity can appear only once and only for an action whose completed grounding issued it. Located retry diagnostics replace blind reselection, and unchanged invalid selections stop early. Repeated model explanations are omitted from selection input; original grounding evidence remains persisted, and authoritative descriptions and prerequisites remain present. Semantic generation now asks for the fewest business actions preserving all requested outcomes and cleanup of concrete acquired resources. The regression suite covers these selection boundaries.

Read-only measurement of the revised selection for Designer 7 is 9,882 estimated input tokens (23,675 prompt bytes and 5,201 schema bytes), below its unchanged 12,000 ceiling; no model was dispatched. Unambiguous actions remain accounted for without model reselection. Full parameter descriptions remain in binding, while selection carries authoritative behavior and artifact prerequisites. The Planning suite now passes 146 tests.
