# Gate 1 — frozen occurrence-boundary LOCAL

## 1. Outcome

**LOCAL PASS.** Exactly one fresh case, `schema5-occurrence-boundaries-diagnostics-1:local`, completed canonical admission on production `cfc058fd52183854d09b2540b9f57d1e337797f9`.

The single required operation is `operation_e226ff9ed819b5a645e7d9de`, kind `local_processing`. Its one `main` result realization consumes canonical `record` and `threshold` and produces `classifiedResult`. Classification rules/conditions/fallback and descriptive local evidence govern that same operation. Preservation remains an output modifier. There are no external, lifecycle or human operations, unresolved runtime scopes, standalone identity questions, or dependency-model questions.

This is fresh interpretation/admission evidence over supplied canonical declaration fixtures. It does not establish fresh declaration convergence, construction, behavior acceptance or Stage-1 success. MIXED and Stage 1 were **not run**.

## 2. First meaningful blocker

None. Two interpretation singletons reached 8,192 output tokens and completed through their existing one-time 16,384 escalations. These were handled output-budget events, not terminal failures. All twelve requests have verified receipts. There was no provider retry, unverifiable dispatch or semantic repair.

## 3. Root cause

No new defect was established. The prior speculative occurrence domain did not recur: this run established one result realization with no invocation-boundary or occurrence-identity decision. Interpretation finished within ten calls, including two escalations, leaving sufficient capacity for the two admission calls.

The model classified two local action contributions. Realization grounding established the classifier effect and deferred the descriptive contribution. One subsequent governing decision attached the description to the already realized effect. The engine then bound both contributions deterministically to the same canonical operation and committed an empty operation-producer proof.

## 4. Authority analysis

Typed host projection supplied **eight engine-owned policy clauses and nine obligations**, with zero host interpretation decisions. Five baseline structural units remained engine-owned contracts, with zero structural interpretation decisions or baseline operations.

Eight user interpretation scopes produced eight normalized runtime evidence items: four contracts, two planning directives and two local contributions. The other thirteen runtime items are the five baseline contracts and eight policy projections. No runtime scope was unresolved. Preservation was already non-executable contract evidence, so the declaration-coverage exclusion count was **zero**; this differs from synthetic fixtures that deliberately supplied mistaken preservation actions.

Effect semantics remained model-grounded: two realization/contribution decisions and one governing decision. Canonical identity and the final governing target binding were deterministic. The governing binding count of one does not erase the preceding model-owned applicability decision. Admission alone owns operation-data dependencies; the singleton's producer set is empty and current `Effect.Producers` is empty. No later relationship or executable-construction stage ran.

## 5. Proposed action

No production change. Recommend **MIXED** as the next gate, under separate authorization. No replacement case, isolated provider experiment, limit increase or automatic next-gate advancement was performed.

Only harness setup, current-proof checks, reporting and associated tests changed. The harness obtains metadata from frozen `AgentPlanningPolicy` and verifies that its complete text and prior policy options are unchanged. The new typed metadata is recorded as the expected configuration addition; fixture source, declarations, catalog, model, budgets and production semantics remained unchanged.

## 6. Validation performed

### Offline prerequisites

- Newly rerun solution: **3,546 passed, zero failed, one optional live-provider skip**; focused harness/planning subset: **195 passed**.
- Harness and test builds passed without warnings, with production-reference rebuilding disabled.
- Published Native AOT planning/structural-baseline/encrypted-restart and trimmed Agent.Server persistence smokes passed.
- Six classifier/batch and eighteen additional reference cases passed without provider or business transport calls. Synthetic local/mixed fixture selfchecks passed separately, including zero-dispatch restart and stale-proof rejection. These were offline checks, not additional live cases.
- Previously validated production build, frontend and four package results were retained and hash-verified. Their earlier 3,534-test result is distinct from this task's rerun.
- One existing-provider USD→EUR prerequisite check succeeded using a quote dated **2026-09-15**. It made zero model calls and supplied no override or injected quote to the live budget.

### Live and read-only evidence

The case started with fresh encrypted state and zero consumed usage. The model was `gpt-5.5-2026-04-24`, with effective reasoning `low` throughout. Input ceiling 12,000, dispatch target 9,600, normal output 8,192, bounded singleton escalation 16,384 and the sixteen-call budget remained unchanged.

After atomic admission, a newly deserialized encrypted checkpoint passed current-proof validation and the completed-admission path using a transport that rejects all calls. The complete snapshot fingerprint, canonical identities, dependency proof and accounting were unchanged: **zero provider calls and zero checkpoint writes**.

Read-only original-schema audit checked all twelve receipts: ten completed responses had zero schema findings; two were verified output limits without assignments. Current committed admission validated again with no provider calls, no receipt substitution and unchanged checkpoint/budget bytes.

Proofs:

- Admission: `925d9fa30da92f6bfaa42530ab7763c3d7f748114cf6d53568e2d4b1ca9ba122`.
- Dependency: `5d1cd5222922dcda52f75d0dd7b3a2a1c2223da0f737448f16104e548a633fda`.
- Read-only snapshot: `1df3996b29429c5572cea51e0dd6c5616697c8b48f548c94cfd1faabefe7ba9c`.

Final integrity checks matched all thirteen recorded production source hashes, twenty-one frozen tested DLL copies, both published executables and four packages. Only the harness and test DLLs were rebuilt. All twenty-three running harness/production DLLs matched the new frozen manifest. Archived campaign/accounting fingerprints remained unchanged.

## 7. Decision accounting

| Phase / responsibility | Distinct decisions | Provider calls | Input | Output | Reasoning |
|---|---:|---:|---:|---:|---:|
| Interpretation | 8 | 10 | 30,988 | 18,103 | 17,348 |
| Realization / contribution grounding | 2 | 1 | 2,065 | 297 | 0 |
| Governing grounding | 1 | 1 | 984 | 242 | 0 |
| Boundary scope | 0 | 0 | 0 | 0 | 0 |
| Occurrence identity | 0 | 0 | 0 | 0 | 0 |
| Operation dependencies | 0 | 0 | 0 | 0 | 0 |
| **Total** | **11** | **12** | **34,037** | **18,642** | **17,348** |

Runtime-facet decisions are embedded in the eight interpretation decisions, not eight additional calls. Eight initial interpretation pages became ten requests through two singleton escalations. There were **zero partitions**, **zero semantic correction pages/repair reservations**, **zero unverifiable dispatches**, and no coordinator-only rejected reservations. All provider usage is known. The durable budget consumed twelve calls, leaving **four**.

The engine made one canonical identity assignment and one final governing attachment, projected eight host clauses/nine meanings and five baseline contracts, and established the singleton's empty operation-producer set. No executable holes were constructed or resolved.

Largest estimated input: **5,648 tokens**. Largest actual input: **4,072 tokens**.

Both escalated requests completed under the permitted 16,384 output allowance, with lineage and exact request/schema/context/receipt fingerprints retained in the [JSON report](planner-occurrence-boundaries-local-1.json):

- `interpret_0e442b68f5c0f13d52325f53`: request 2 → request 3.
- `interpret_62ab0dac77537c06b2eaa41c`: request 4 → request 5.

Compared with the retained LOCAL checkpoint, interpretation decisions are 17→8, host questions 9→0 and initial pages 9→8. The observed run used twelve total calls; synthetic envelope counts are not provider-call savings, and this single run does not guarantee future truncation frequency or headroom.

## 8. Next gate

**MIXED, requiring separate authorization.** Stop here. LOCAL admission passed; MIXED, Stage 1, behavior/artifact approval and executable execution cases remain not run.
