# Canonical contract coverage before executable qualification

## 1. Outcome

**IMPLEMENTED — OFFLINE VALIDATION PASSED.** Contribution proof **3** and operation-admission proof **13** now bind role-neutral contract eligibility. Coverage 3, effect 7, applicability/dependency/occurrence 1, runtime 7, source 6, declaration 5, baseline projection 1 and Schema-5 storage remain unchanged.

This implementation changes production from `23bce4bc842e598d19866d92d497772e1f3c0538` on repository base `00c7990fa136ab18e90b19a59110268395eaa101`. No live LOCAL, MIXED, Stage 1 or provider request ran. The historical MIXED campaign remains stopped and unaccepted. Full fingerprints, test summaries and separate evidence tracks are in [the machine-readable report](planner-canonical-contract-coverage.json).

## 2. First meaningful blocker

The retained MIXED response to `contribution_runtime_30713b159af7b4abb37970db` qualified `preserve` `[453,461)` as external-read support, although the complete preservation evidence `[453,505)` was already canonically attached to the output declaration. That is the first verified qualification-domain defect. The `to load.` evidence `[82,90)` is also wholly inside canonical declaration evidence `[29,90)`.

The later `DIAGNOSTIC_GOVERNING_EVIDENCE` assertion separately required an `attach` record for the classification rules. A positive support already owned the complete `[255,385)` rule clause. That harness assertion was too specific about representation, but correcting it alone could not accept the invalid preservation support.

## 3. Root cause

Declaration exclusions were restricted to preliminary `local_behavior`. An erroneous `runtime_action / external_read` classification bypassed established contract ownership. Both initial qualification and restored admission inherited that incomplete authority boundary.

The new index resolves current root, alias, modifier and omission-default assignments to exact source owner, source identity, content fingerprint, coordinates and canonical declaration targets. It never expands coverage through `ClauseReferences`.

## 4. Authority analysis

Canonical declarations own contract spans for every preliminary runtime role and kind. Full coverage produces a deterministic exclusion before occurrence or contribution requests. Disjoint actions retain existing positive-qualification requirements. A partially overlapping parent can proceed only when current, separately owned, disjoint action evidence completely accounts for its executable remainder with compatible execution facts. The parent remains auditable through a deterministic separation outcome; no substring is manufactured and no remainder is dropped.

The same eligibility derivation protects occurrence domains, contribution schemas, selected subspans, coverage reconstruction and completed-admission validation. A smaller covered subspan or borrowed occurrence anchor cannot regain authority. Sorted coverage references and current declaration fingerprints enter the versioned eligibility domain and contribution/admission fingerprints. No graph, service, phase or authoritative snapshot collection was added.

Positive support, property applicability, necessity, resource/baseline ownership, scoped dataflow and admission-owned dependency validation remain mandatory. Eligibility does not prove execution.

## 5. Implemented action

- Updated existing operation coverage, occurrence, effect, contribution and admission validators; canonical operation IDs remain effect-owned and unchanged.
- Corrected harness rule acceptance to require exact owned semantic coverage through current positive support or proven governing applicability. Containing-clause context alone is insufficient.
- Kept descriptive properties governing-only and inspected every support contribution for preservation overlap, including support not used as the diagnostic anchor.
- Added the offline-only `audit-contract-coverage-fixture` command. It uses detached encrypted inputs and production builders with synthetic transport, never a provider, persistent reservation or checkpoint writer.
- Preserved historical reports, receipts, schemas, checkpoint and budgets. Another verified failure in this contract-authority class requires architecture review before a further targeted correction.

## 6. Validation performed

**Offline:** 3,680 solution tests passed across 29 assemblies; one optional provider test skipped; zero failures. This includes 1,085 planner, 855 Flow Core and 542 Agent.Server checks. Focused runs passed 85 planner checks, all 21 new coverage cases and 197 harness checks; these overlap the full suite and are not added to its total.

The warning-free solution/harness builds, Agent and Flow frontend builds, four component packages, Native AOT planning/encrypted-restart smoke, trimmed Agent.Server persistence smoke, harness selfchecks and reference checks passed. Reference checks covered six classifier/batch cases and eighteen frozen fixtures without business transport. Existing documented Jint/interactive-host publish exceptions remain unchanged; no suppression was added.

**Historical replay:** all 16 retained answers still validate against their original schemas with zero findings. The preserved production harness completed old-proof re-entry with zero calls. Current-proof replay stops at `INTENT_OPERATION_PROOF_MISSING`, also with zero calls. No old receipt was interpreted under a new request domain.

**Synthetic corrected LOCAL:** one required `operation_e226ff9ed819b5a645e7d9de` (`local_processing`), `record + threshold → classifiedResult`, four legitimate supports and four governing properties. Descriptive evidence supplies no support. Preservation remains canonical declaration evidence; no external operation exists.

**Synthetic corrected MIXED:** exactly two required operations: `operation_6b515f252d71b693829d2fe4` (`external_read`) and `operation_e226ff9ed819b5a645e7d9de` (`local_processing`). The read consumes `sourceId`; the local effect consumes the read result and `threshold`, producing `classifiedResult`. The admission proof alone establishes read → local, with owned semantic evidence. There is no self-edge, cycle or other operation. Public ports, optional threshold/default `100`, enum and preservation attachments remain fixture-owned and unchanged. The read's intermediate/dataflow mapping is explicitly synthetic corrected evidence, not retrospective approval of its archived mapping.

Both synthetic fixtures use retained eligible qualification shapes as labelled synthetic answers to new domains, never as matching historical receipts. Both validate current proofs, reload/re-enter with zero calls and writes, and preserve canonical IDs, contracts, proof/accounting/snapshot fingerprints. The MIXED relationship path projects the admitted data edge and exposes no `data` response alternative. The read has one positive support and three properties attached through deterministic occurrence ownership. The local transformation has one complete rule-clause support, which owns its conditions and fallback without a duplicate governing record.

The archived checkpoint remains `389027200228f43bc8c848de1f948647ccb550c2199dab9c2467d7dea19019e9`; aggregate archive fingerprint remains `9a7532da44586206221f146e907eea33d0ee0bd3334b2f341db549636e942199`. All 23 preserved diagnostic binaries, retained published artifacts/packages, declaration fixtures and original reports match their prior hashes.

## 7. Decision accounting

Measurements below use the unchanged production packer and explicitly synthetic first-completion answers. They are not live convergence or measured provider-call savings.

| MIXED work | Retained decisions / calls | Corrected decisions / nominal requests |
|---|---:|---:|
| Interpretation | 9 / 9 | 9 / 9 |
| Contribution qualification | 4 / 4 | 2 / 2 |
| Realization coverage | 2 / 1 | 2 / 1 |
| Governing applicability | 1 / 1 | 0 / 0 |
| Admission dependencies | 2 / 1 | 2 / 1 |
| Boundary scope / occurrence identity | 0 / 0 | 0 / 0 |
| Through admission | 18 / 16 | 15 / 13 |
| Downstream relationships | Not reached | 8 / 1 |
| Complete diagnostic path | Incomplete | 14 requests |

Exactly two contract-covered qualification decisions disappear before dispatch. The preservation-only applicability question also disappears. Both canonical identities remain deterministic. MIXED has three contract-covered runtime records in total: the two newly excluded candidates and the already excluded output evidence. Its three read-property attachments use deterministic owner proofs; this does not turn arbitrary singleton applicability into proof. Dependency assessment remains two evidence-backed pair decisions, with one positive read → local edge; source order and endpoint count confer no authority.

Corrected MIXED has **3 nominal slots at admission** and **2 after relationships**. LOCAL has eight interpretation, three qualification, one coverage and four applicability decisions: **12 nominal requests**, with zero identity/dependency-model decisions and 4 nominal slots remaining. No budget, reasoning, input/output limit, partition rule or escalation rule changed. These counts provide no guarantee against future truncation, escalation or repair.

The largest measured post-interpretation envelopes are 2,325 estimated input tokens for LOCAL and 2,207 for MIXED; the MIXED relationship request is 1,644. Interpretation retains its eight/nine packed initial pages. These are estimator values, not provider usage.

This iteration made zero provider calls and used no provider tokens. Synthetic envelopes have no actual provider usage. Historical MIXED remains 16 verified calls, 35,076 input tokens, 4,933 output tokens (2,636 reasoning), with zero partitions, escalations, semantic repairs or unverifiable dispatches. Archived accounting was not reset or replenished.

## 8. Next gate

**One fresh LOCAL, requiring separate authorization.** Stop after these offline results. No live validation or retrospective MIXED acceptance is included.
