# Required governing-evidence ownership

This checkpoint implements the renewed authorization following the stopped
`schema5-autonomous-execution-request-1` campaign at `2e4a95a`. The required offline
gate passed; this document does not claim live convergence.

## Authority boundary

Canonical execution requests and declaration assignments retain their existing
exclusive authority. The engine subtracts their exact coverage from each eligible
contribution clause. Existing non-whitespace lexical spans are split further at
runtime ownership and semantic grounding edges. Punctuation and source coordinates
are retained, including an ownership edge inside a word.

Every remaining portion has one required response key. The bounded answer selects
a governing kind or, when no established condition/fallback contradicts it, an
explicit no-operation-relevance exclusion. The unresolved response remains
available. Required coverage establishes accounting only: semantic-kind validation,
applicability to a realized effect, and independent business review still apply.

Adjacent answers may share a response-local group. Projection merges only equal
meanings with equal ownership and grounding, across whitespace alone. Persistent
identities derive from the normalized resulting evidence and meaning, never the
group label. Declaration islands and distinct ownership remain separate. Existing
policy applicability is reused only when the union of current, policy-grounded
portions covers every required source position for the same realized target;
normalization must not create a second semantic relationship decision.

Optional nested qualifiers retain their fixed request parent and contained evidence
domain. They cannot cover required residual keys. The total projected clause still
has at most six units. Current proofs are request **1**, contribution **8**, admission
**18**; other proof domains and Schema-5 persistence are unchanged.

## Alternatives and review

The cumulative [mission journal](planner-autonomous-convergence.md) records the
renewed authorization and ten-point pre-patch authority review. Three structural
options were compared: required residual answers after requests; joint clause-level
request/property answers; and a semantic partition before request qualification.
Required residual ownership removes omission authority while preserving the
already established immutable execution-request boundary.

## Harness

The next campaign is `schema5-autonomous-residual-ownership-1`. Its preparation must
bind the validated production commit, hashes, fixture/catalog fingerprints,
configuration and permitted gates before dispatch. Existing namespaces and public
approval commands remain unchanged. A retained checkpoint or progressive start
reservation is reconciled instead of reacquired. Completed gates return existing
results without provider calls or checkpoint writes; ambiguous journal dispatch
state fails closed.

Independent review reconstructs residual coverage separately and rejects executable
property authority. LOCAL and MIXED both require exact fallback applicability to
the classifier. Progressive behavior and artifact approvals continue to require
exact revisions and hashes and all 6 / 9 / 18 execution cases.

## Evidence

Historical: the stopped campaign retains ten calls, 30,230 input tokens, 2,815 output
tokens and 1,570 reasoning tokens. Its 22 production DLLs and 29 earlier validation
artifacts were verified unchanged. The original binary reproduces the original
coverage failure; the new binary refuses to use those property receipts as current
authority. Both audits make zero provider calls and preserve the checkpoint.

Synthetic: corrected detached fixtures use explicitly authored test answers. They
are never receipts, live success, or input to a fresh session. The final LOCAL
fixture passed with 12 nominal calls, four semantic units and one property page.
Its two property schemas contain 6,191 and 2,490 bytes; the largest estimated
request input is 4,751 tokens. Restart preserves the complete snapshot and admission
fingerprint with zero calls and writes. MIXED passed with 15 nominal calls (14
through admission plus one downstream relationship page), five semantic units and
one property page. Its property schemas contain 3,307 and 4,751 bytes; the largest
estimated input is 4,600 tokens. Its exact fallback ownership and read-only restart
also pass. Both fixtures preserve the original archive and action references.
Source, binary and artifact hashes are recorded in the accompanying
[machine-readable evidence](planner-residual-ownership.json).

Live: no new campaign has dispatched. All original budgets and the post-redesign
recurrence hard stop remain in force.

## Completed offline validation

The solution suite passed **3,839 tests**, with no failures and one
existing opt-in external-publication test skipped. This includes 1,157 planner
tests and 627 host tests. Focused ownership, applicability, containment and
mission re-entry regressions passed before the full suite. The solution and
harness builds, four package builds, both server frontends, 281 Python core tests,
23 Python CLI tests and protocol checks passed. Native AOT encrypted restart and
trimmed EF persistence passed against the published binaries. Desktop packaging
verified the host, UI, native runtime assets, Chromium and four Native AOT tools.
Build and publish results contain no warnings under the unchanged documented
exceptions. Reference selfchecks passed without model or business transport calls.

The report freezes 121 production-source hashes, 23 DLL hashes and
71 validation-artifact hashes. Superseded offline test runs are retained and
labelled, including the corrected policy-reuse integration failure, obsolete test
answer shapes and the rejected preliminary synthetic MIXED fixture. They are not
accepted live evidence.

Next: commit and push this stable redesign, bind the campaign to that production
commit using the existing encrypted manifest, preserve its frozen binaries, then
run exactly one fresh LOCAL. MIXED and all three full workflow tiers remain pending.
