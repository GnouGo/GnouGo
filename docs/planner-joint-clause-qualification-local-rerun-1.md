# Joint clause qualification — fresh LOCAL rerun 1

## 1. Outcome

**LOCAL PASS.** Campaign `schema5-joint-clause-qualification-diagnostics-rerun-1:local` established exactly one required canonical `local_processing` operation:

`record + threshold -> classifiedResult`

Canonical operation: **`operation_e226ff9ed819b5a645e7d9de`**. It has one `main` result realization, one positively qualified execution support and one proven governing property. No external, lifecycle or human operation exists. Admission committed atomically; encrypted reload and completed-admission re-entry preserved the complete snapshot and proofs with **zero provider calls and zero checkpoint writes**.

Production remains frozen at **`d2ceac7cd85793baea732721f4e2b092c94be210`**. No production code changed. This establishes admission convergence using the explicitly supplied declaration fixture. Fresh declaration convergence, executable construction and Stage-1 success are outside this gate. MIXED and Stage 1 were **not run**.

[Detailed redacted evidence, fingerprints and validation logs](planner-joint-clause-qualification-local-rerun-1.json).

## 2. First meaningful blocker

**None.** All twelve requests have verified receipts. There were no unresolved runtime/effect scopes, unverifiable dispatches, partitions, escalations, semantic repairs or exhausted budgets. Four of the sixteen durable call slots remained at admission completion.

The earlier stopped LOCAL and its unknown usage remain unchanged. The separately successful isolated request was not imported or replayed into this fresh campaign.

## 3. Root cause / observed behavior

The complete classification-rules clause supplied a requested-execution unit with its predicate and owned support reference. The complete deterministic/local/in-memory sentence supplied a governing-property unit and zero support. No fragment selected executable support independently of a qualified execution request.

The live interpretation produced three preliminary executable candidates. Canonical declaration coverage excluded preservation before qualification. The remaining rules and descriptive clauses required **two joint qualification decisions**. This differs from the earlier synthetic three-clause checkpoint because the live interpretation treated the introductory workflow-authoring clause as a planning directive. The classification request is fully established by the rules clause; no privileged introductory sentence is required.

This is a measured live result, not a claim that every future interpretation will produce this same decomposition or request count.

## 4. Authority analysis

- Canonical declarations own `record`, optional `threshold` with omission default **100**, and required `classifiedResult`. The category and preservation attachments remain on the output declaration.
- Current contribution proof **4** supplies execution authority through the requested-execution unit. Preliminary runtime labels and historical `EvidenceRole` grant none.
- Coverage proof **3** establishes the single result realization and public dataflow from that support.
- Applicability proof **1** establishes the descriptive property's target after realization. One plausible target still received a semantic model decision; singleton cardinality was not used as proof.
- Operation identity is a deterministic projection of the canonical result effect. There were **zero standalone identity decisions**.
- Admission dependency proof **1** establishes an empty operation-producer set deterministically. There were **zero dependency-model decisions**, and `Effect.Producers` supplied no authority.
- Admission proof **14** validated and committed the combined result. Effect proof **7** and Schema-5 storage remain unchanged.

Execution support:

| Field | Evidence |
|---|---|
| Contribution | `contribution_89d6539a7a8120b34cb2bf1c` |
| Owned reference | `r_f988a5360df02f5f721b9dec`, request `[325,443)` |
| Qualified unit | `unit_c63b3077c09dfce1c057ab11`, `requested_execution` |
| Positive basis | `requested_result_production` |
| Canonical effect | `operation_e226ff9ed819b5a645e7d9de` |
| Origin | `ModelQualification`; support projection is deterministic |

That support owns the complete rejected/high/standard classification rule, including its conditions and fallback. A duplicate governing record is not needed to retain those semantics.

Governing property:

| Field | Evidence |
|---|---|
| Contribution | `contribution_05534db2084ba50820948d98` |
| Owned reference | `r_46863c092754d9f853a1ca0a`, request `[539,599)` |
| Qualified unit | `unit_27fdaa278ae70bb5102714f8`, `governing_property` |
| Preliminary runtime kind | `local_processing` |
| Applicability target | `operation_e226ff9ed819b5a645e7d9de` |
| Applicability origin | `ModelApplicability` |
| Applicability proof | `6311b9363cab36eec5d1f597a108abc20061408bf8b31d8659be990fda3986d2` |

The entire descriptive sentence, including its framing, is governing-only. Its attachment is a deterministic projection of the model applicability proof. The legacy `governingModelDecisions = 0` field counts assignment origins; it must not hide the **one actual applicability model decision**. This live run's preliminary descriptive kind was local; wrong-kind behavior remains covered by the offline regressions.

## 5. Proposed action

Stop after LOCAL acceptance. Recommend **one fresh MIXED**, requiring separate authorization. Do not start it automatically and do not start Stage 1.

No production patch, prompt experiment, limit increase, replacement start or provider retry was performed.

## 6. Validation performed

**Fresh prerequisites:** the complete solution passed **3,695 tests**, with **one optional provider-test skip and zero failures** across 29 assemblies. This includes **1,092 planner** and **550 Agent.Server** tests. The **151 focused harness checks** passed. Harness/test builds used `BuildProjectReferences=false` and produced zero warnings or errors.

Both published Native AOT planning/encrypted-restart and trimmed Agent.Server persistence smokes passed. Synthetic harness selfchecks passed, including detached zero-call/zero-write restart. Six classifier/batch and eighteen frozen reference cases passed without provider/business transport. Previously completed frontend, package and publish evidence was hash-verified and retained; those builds were not repeated for this identity-only gate.

The existing exchange-rate provider checked **USD → EUR once**, accepting a quote dated **2026-09-16**. The quote was not injected into planning. Freeze verified unchanged model `gpt-5.5-2026-04-24`, all-low reasoning, 12,000 input ceiling, 9,600 dispatch target, normal 8,192 output, bounded 16,384 singleton escalation, existing monetary/token/deadline/repair limits, catalog, policy and declaration fixture.

**Live acceptance:** eight typed host-policy clauses projected nine obligations with zero host interpretation decisions. Five baseline structural units projected five engine-owned contract records with zero baseline interpretation decisions or operations. There were zero unresolved runtime scopes. The public declaration fixture retained its two inputs, one output, omission default and attachments.

**Persistence:** the committed checkpoint was deserialized into detached state. Current contribution, coverage, applicability, effect, dependency and admission proofs validated. Completed re-entry used a rejecting transport and rejecting checkpoint writer: **zero calls, zero writes**, identical operation ID, proof fingerprints, accounting and snapshot.

Key fingerprints:

| Proof / state | Fingerprint |
|---|---|
| Complete snapshot | `f50b3f708bf4c7751e3d263d416432488cecb640b640cd7b862309cf83184db7` |
| Aggregate admission | `e63649ef673ea11b178511bf0d7ef1e1fb2d4a323e0861940fe64484321aa001` |
| Per-operation admission | `8fdb0e25286664810c5882d6b764acd698a19d7f851e69079c135658a91fcc3c` |
| Coverage | `2516c848f61b9c96012f90bf08be0c10e69fea19c507228ef78f88e7d3183102` |
| Dependency | `ef1dcaa35f165a6c277cc9f07758e96e15f50466eca1d3abc70bcc50e82f4fdc` |

All **17 production-source hashes**, **22 production DLL hashes**, fixtures and historical report hashes matched before and after execution. The archive aggregate remained `a7a1a8673172b3f873daea92d9466e5525baad1563c5a108526f16d1b063a1d1`. Read-only audit of the prior stopped case left its checkpoint, original missing receipt and accounting unchanged. The new report reloads identically.

## 7. Decision accounting

| Class | Model decisions | Verified provider calls | Input | Output | Reasoning |
|---|---:|---:|---:|---:|---:|
| Interpretation | 8 | 8 | 24,736 | 1,852 | 1,136 |
| Joint clause qualification | 2 | 2 | 2,691 | 600 | 357 |
| Realization coverage | 1 | 1 | 911 | 166 | 37 |
| Governing applicability | 1 | 1 | 499 | 89 | 0 |
| Boundary/scope grounding | 0 | 0 | 0 | 0 | 0 |
| Standalone occurrence identity | 0 | 0 | 0 | 0 | 0 |
| Operation dependencies | 0 | 0 | 0 | 0 | 0 |
| **Total** | **12** | **12** | **28,837** | **2,707** | **1,530** |

Interpretation had **8 initial pages**. Its eight runtime facets were embedded in those calls, not extra requests. Qualification selected **one execution-request unit and one governing-property unit**. Canonical identity assignment, support projection and the final attachment projection were deterministic; the property's semantic applicability was model-owned.

There were **19 deterministic contribution exclusions**, including contracts, typed policies, baseline structure, the planning directive and declaration-covered preservation. These are accounting outcomes, not nineteen saved provider calls. The live run had one support and one governing attachment; acceptance still permits multiple valid supports for one effect.

Twelve coordinator reservations, twelve durable budget charges, twelve journal requests and twelve verified receipts agree. All current-run usage is known. Output includes reasoning; output minus reported reasoning is **1,177 final-answer tokens**.

- Remaining allowance at admission and completion: **4/16 calls**.
- Largest input: **5,549 estimated**, **3,997 actual** tokens.
- Partitions / singleton escalations / semantic repairs / unverifiable dispatches: **0 / 0 / 0 / 0**.
- New planner decisions introduced by this harness gate: **0**.
- Historical missing usage remains unknown and is not included as zero in this fresh campaign's totals.

## 8. Next gate

**One fresh MIXED, requiring separate authorization.** LOCAL is complete. No further live execution.
