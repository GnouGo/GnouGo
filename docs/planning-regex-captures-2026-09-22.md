# Workflow Designer: regex capture typing

Tested implementation: `9142625f70770a04e97ff044ab5f03bcd6cb6f3a` on `feat/semantic-grounded-planner`.

Designer session `7d5fb82aed7d42239e8ffb7ae12e91b4`, trace `2b3877048d6d528d16ba8ed4c399a488`, stopped with `GROUNDED_CONTRACT_INVALID` after seven calls and two replans. Its final calculation called `.replace()` on regex captures that the checker treated as opaque. A later `value.validate` could not establish types inside that earlier calculation. The replan allowance was exhausted; the eight-call allowance was not. This failure is separate from the saved-history compatibility exception.

Literal regex `match()` on a statically typed string now infers an array of nullable strings, or null for no match. Capture aliases and string-method chains retain that information. Member validation rejects invented properties and incompatible union alternatives. Dynamic patterns and opaque receivers remain conservative; absent captures and out-of-range indexes can still fail at runtime. Producer contracts, runtime validation, public interfaces, prompts and limits are unchanged.

A sanitized reproduction failed before the correction. All **859 Flow, 194 Planning, 76 Integrations and 389 Agent.Server tests pass (1,518 total)**. Regressions cover aliases, replacement chains, optional and unmatched captures, invalid input, global matches, missing indexes, opaque receivers and dynamic patterns. Validation, lowering, compilation and isolated execution pass; a mock downstream call is never reached on missing or invalid values. The solution/frontend build and Native AOT publish are warning-free. All eight Native AOT corpus cases pass, including their execution assertions.

Read-only revalidation of the unchanged saved candidate passes scoped capability bindings, business-output contracts, grounded validation, graph checks and compilation with its required host confirmation gate. **No remaining diagnostics were found in those checks.** This candidate contains seven operations for the first two semantic actions; it is not the completed workflow. No remaining binding generation, scenario generation or scenario execution was attempted for this session.

The session remains stopped at revision 16, with seven calls, two replans and 71,359 total tokens. All 129 capabilities were covered across two catalog pages. Its unchanged 680-byte prompt has SHA-256 `8f9c138913d4fd8e136760df3b7e20fe4cf7623f02fa9fa4c46220bcaae1cf85`; the catalog hash is `3635f655617248b7ec5ab9d4ec793264d70325dc44ade8a58d82eec44b109634`. Public encrypted-storage APIs supplied the evidence; private payloads stayed in memory. Counts and content hashes for ten session/request/receipt/budget collections match before and after, including historical uncertain reservations.

No fresh model dispatch, approval or external workflow execution occurred. The [earlier campaign](planning-semantic-grounding-live-2026-09-21.md) remains unchanged, including its unmet overall acceptance criterion. This correction does not establish that subsequent generation will reach `FinalReview`. [Sanitized evidence](evidence/planning-regex-captures-2026-09-22.json) records settings, coverage, accounting, validation results and verification hashes. Documentation commits following the implementation do not change the tested source.
