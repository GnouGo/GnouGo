# Runtime evidence grounded in source authority and canonical declarations

ConstraintsOnly sources no longer answer a runtime facet. The engine records policy scope and engine-source-authority origin for each issued source scope; semantic policy classification remains model-owned. Old unresolved or model-origin host scopes cannot grant current proof.

Canonical operation admission checks current declaration assignments before creating or reusing local occurrences. It uses the exact owned root/alias/modifier/default evidence, never an expanded context clause. Full coverage excludes a preliminary local occurrence while preserving the governing contract evidence. Separate action spans remain executable. Partial overlap without an independent action span stops with a located diagnostic; no text is cropped or inferred.

The internal local-processing representation, native skeleton, declaration root/attachment semantics, confirmation enforcement and external capability boundaries remain unchanged. Runtime evidence proof is version 2; operation admission proof is version 3. Source proof 4, declaration proof 5 and Schema-5 encrypted storage are unchanged. Current declarations are revalidated at the admission boundary; operation identity remains anchored to the same owned action. Restart recomputes coverage and reuses receipts without duplicating accounting.

## Evidence and diagnostics

The original `schema5-runtime-admission-diagnostics-1` remains immutable. Read-only audit revalidated all eight completed receipts against their original schemas, with no provider calls. It retained the policy `unresolved` response, preliminary preservation occurrence and pre-journal ninth reservation separately. Changed request schemas cannot reuse those receipts as new evidence.

The new `schema5-runtime-evidence-diagnostics-1` permits one local case followed, only on success, by one mixed read/local case. Both supply explicitly labelled canonical declaration preconditions with exact source attachments. Fresh semantic/runtime receipts remain encrypted and unmodified; the harness replaces only the declaration projection and preserves non-declaration obligations. No fresh declaration convergence or full Stage-1 success is claimed.

Each case has a durable 16-dispatch budget covering interpretation, admission, relationship decisions, corrections, partitions and escalation. Preflight uses production request packing. Normal model, reasoning, token, monetary and deadline settings remain unchanged. Completed receipts are reused; unverifiable requests and blocked cases cannot retry. The mixed case checks the existing producer-to-consumer relationship path. No planning session, behavior acceptance or executable construction is started.

Reports count the omitted ConstraintsOnly runtime facets separately from local occurrences excluded by declaration coverage. Neither number is reported as a provider-call saving. Coordinator reservations, journal requests, receipts and unavailable usage remain distinct.

Offline and isolated diagnostic results will be recorded with the tested commit and frozen binary/input fingerprints. The first meaningful diagnostic blocker ends the sequence without a patch or replacement run.

## Offline validation results

The complete solution passed **3,370 tests** across 29 projects; one existing optional live test was skipped. Planner tests: **901**. Agent.Server tests: **416**. Solution and harness builds, both affected frontends, all four packages, Native AOT planning/encrypted restart and trimmed Agent.Server EF persistence passed without warnings under documented exceptions. No new warning suppression was added.

The isolated declaration/admission fixture selfcheck passed both local and mixed cases with zero model calls. Independent classifier/batch and CodeReview reference selfchecks passed 6 and 18 cases respectively. These are offline evidence, not live admission or Stage-1 convergence. See [offline results and hashes](planner-runtime-evidence-offline.json).

## Frozen isolated diagnostic result

The implementation and binaries were frozen at **`04d62785ca6e21d2493074df8bd2d57113629b37`**. Model, transport configuration, source budget settings and catalog fingerprints match the previous diagnostic. The effective case budget is 16 durable dispatches, with all-low reasoning and unchanged 12,000/9,600 input and 8,192/16,384 output bounds.

Exactly one fresh **local** diagnostic ran. It completed all 11 interpretation pages and stopped at canonical admission with **`INTENT_OPERATION_UNRESOLVED`**. The mixed diagnostic was **not run**. No planning session, BehaviorReview, construction, Stage 1 or Stage 2 was started.

| Measurement | Local diagnostic |
|---|---:|
| Verified model calls / journal requests / reservations | 11 / 11 / 11 |
| Unverifiable requests | 0 |
| Input / output / reasoning tokens | 34,598 / 4,854 / 3,146 |
| Largest estimated / actual input | 5,353 / 4,090 |
| Completed pages / source decisions | 11 / 22 |
| Output partitions / singleton escalations | 0 / 0 |
| Semantic repairs, all gates | 0 |
| Engine-owned policy runtime scopes | 9 |
| Model-owned policy runtime decisions removed | 9 |
| Canonical declaration-covered local occurrences excluded | 1 |
| Unresolved runtime scopes | 0 |
| Canonical operation set | Not committed |

The explicit declaration preconditions remain required `record`, optional non-nullable `threshold` with omission default `100`, and required `classifiedResult`. The output retains its enum and preservation attachments. These are the frozen fixture declarations, not fresh live declaration convergence.

The two targeted corrections are exercised by live evidence: every host runtime scope is engine-owned policy, and `Preserve the original id and amount.` is excluded from operation admission using its exact canonical attachment. The nine removed runtime facets are **not** nine saved provider calls; the semantic-obligation facet still requires interpretation.

The first blocker is `/operations/@r_c44d67bf9faee797793e66b2`: “Governing runtime evidence has no established compatible occurrence.” The model classified `This is deterministic, local, in-memory business processing.` as governing local evidence but selected that same clause as its subject. Neither staged root has that subject reference, so the existing admission check fails closed before any canonical set commits.

Two local roots were staged: `classifying a single record.` and the full classification-rules clause. They are separate preliminary distinct occurrences, **not** a successfully admitted two-operation workflow. No external, lifecycle or human operation was staged. The required one-local-operation result was therefore not obtained. The mixed case has no canonical operation set or live measurements because its gate did not open.

Read-only replay reproduced the same blocker with **zero provider dispatches**, revalidated all 11 responses against their original schemas, and verified unchanged persisted state. No synthetic response replaced a receipt. All 23 frozen DLL hashes and production source hashes remained unchanged, and the diagnostic verified archived accounting—including the previous isolated run—unchanged. No production patch, replacement diagnostic or retry followed the stop.

Artifacts: [frozen manifest](planner-runtime-evidence-manifest.json), [redacted report](planner-runtime-evidence-report.json), [blocker and strict admission replay](planner-runtime-evidence-blocker.json).
