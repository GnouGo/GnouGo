dsl: 1
name: mcp-discovery-real
workflows:
  main:
    steps:
      - id: discover
        type: mcp.list
        input:
          servers: ["*"]
          include: [tools, prompts, resources]
    outputs:
      status: "${data.steps.discover.status}"
      docs: "${data.steps.discover.text}"

